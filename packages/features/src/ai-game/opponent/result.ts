// Re-exported so existing importers keep working; the canonical home is
// `utils/result` (promoted once a second consumer appeared, as this file's
// original TSDoc anticipated).
export type { Result } from "../../utils/result";
export { err, ok } from "../../utils/result";
