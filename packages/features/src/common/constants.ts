export { FILES, RANKS, DISPLAY_RANKS } from "@blindfold-chess/types";

/** Number of files (and ranks) on a standard 8x8 chess board. */
export const BOARD_SIZE = 8;

/** Total number of squares on a standard 8x8 chess board. */
export const TOTAL_SQUARES = 64;

/** Largest valid 0-based file or rank index on a standard 8x8 board (= BOARD_SIZE - 1). */
export const BOARD_LAST_INDEX = 7;
