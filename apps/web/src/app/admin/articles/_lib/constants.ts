/**
 * CSS class applied to `<img>` elements inside the Tiptap editor.
 *
 * Used both when inserting a new image (via `useImageUpload`) and when
 * replacing an upload placeholder with the final URL. Ensures consistent
 * styling between the editor preview and the stored Tiptap JSON output.
 */
export const IMAGE_CLASS = 'rounded max-w-full';
