import { commonMetadata } from './metadata.common';

export const metadata = {
  ...commonMetadata,
  title: 'Compreender as cores das casas',
  tags: [],
  excerpt: 'Domine o padrão de cores alternadas do tabuleiro para uma melhor coordenação de peças',
};
