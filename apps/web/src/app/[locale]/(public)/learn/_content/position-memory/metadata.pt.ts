import { commonMetadata } from './metadata.common';

export const metadata = {
  ...commonMetadata,
  title: 'Como memorizar posições',
  tags: ['memory', 'patterns', 'advanced'],
  excerpt:
    'Aprenda técnicas para memorizar posições de xadrez de forma eficiente usando reconhecimento de padrões e chunking.',
} as const;
