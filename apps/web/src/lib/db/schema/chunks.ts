/**
 * Chunks — catalog of named chess piece-coordination patterns.
 */
export { chunks, positionChunks } from './tables';
export type { Chunk, NewChunk, NewPositionChunk, PositionChunk } from './tables';
